apiVersion: v1
kind: Service
metadata:
    name: "myakscluster-c8e8"
    labels:
        app: "myakscluster-c8e8"
spec:
    type: LoadBalancer
    ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
    selector:
        app: "myakscluster-c8e8"